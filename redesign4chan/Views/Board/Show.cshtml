@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <div>
        <h1>@Model.Name</h1>
        @{
            foreach (var thread in Model.Threads as ICollection<Thread>)// dette forloop bliver kørt lige så mange gange som der er threads i det board der er blevet trykket på
            {
                <h3>@thread.content</h3>//printer contentet af threadsne i boarded
            }
        }
    </div>
</body>
</html>
